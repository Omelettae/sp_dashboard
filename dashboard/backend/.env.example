# Copy to .env and fill in. .env is gitignored - do not commit it.
PORT=3000

DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password

# sensor_dashboard (v2) or sensor_dashboard_v3 (v3)
DB_NAME=sensor_dashboard
